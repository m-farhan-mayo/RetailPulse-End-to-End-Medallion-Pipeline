name: 'retailpulse'
version: '1.0.0'

profile: 'retailpulse'

model-paths: ["models"]
macro-paths: ["macros"]

models:
  retailpulse:

    staging:
      +materialized: view
      +schema: staging

    silver:
      +materialized: table
      +schema: silver

    gold:
      +materialized: table
      +schema: gold